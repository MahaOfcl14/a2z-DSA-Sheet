class Solution {
public:
    int largestElement(vector<int>& nums) {
        int large = nums[0];
        for(int i=1; i<nums.size(); i++){
            if(nums[i] > large){
                large = nums[i];
            }
        }
        return large;

    }
};
